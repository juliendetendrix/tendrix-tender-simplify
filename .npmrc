# Stagehand v2 impose un peer zod légèrement plus strict que la version du projet
# (écart de patch négligeable). On accepte cet écart pour que l'install passe
# partout (local, Vercel, Trigger.dev).
legacy-peer-deps=true
